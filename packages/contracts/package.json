{
  "name": "@fab-o2o/contracts",
  "version": "0.0.1",
  "private": true,
  "description": "Shared API contracts - types, schemas, endpoints",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "rimraf": "^5.0.7",
    "typescript": "^5.5.0"
  }
}
